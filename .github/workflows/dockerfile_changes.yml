name: Check Dockerfile Changes

on:
  push:
    paths:
      - '**/Dockerfile'


permissions:
  contents: write
  packages: write


jobs:
  check_dockerfiles:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: tj-actions/changed-files@v45
        id: changed-files
        with:
          files: '**/Dockerfile'

      - id: set-matrix
        run: |
          CONTEXTS=$(echo '${{ steps.changed-files.outputs.all_changed_files }}' | jq -R -s 'split("\n") | map(select(length>0)) | map({dockerfile_context: (. | sub("/[^/]+$"; ""))})' | jq -c .)
          echo "List of Dockerfile contexts: $CONTEXTS"
          echo "matrix=$CONTEXTS" >> $GITHUB_OUTPUT

  run_docker_build:
    needs: check_dockerfiles
    if: ${{ fromJson(needs.check_dockerfiles.outputs.matrix).dockerfile_context[0] }}
    strategy:
      matrix: ${{fromJson(needs.check_dockerfiles.outputs.matrix)}}
    uses: ./.github/workflows/image_publish.yml
    with:
      docker_context: ${{ github.workspace }}/${{ matrix.dockerfile_context }}
      tag: nightly
    secrets: inherit


  log_results:
    needs: run_docker_build
    runs-on: ubuntu-latest
    steps:
      - name: Log Build Results
        run: |
          if [[ ${{ needs.run_docker_build.result }} == 'success' ]]; then
            echo "Docker builds completed successfully."
          else
            echo "Some Docker builds failed. Please check the logs for more information."
          fi
